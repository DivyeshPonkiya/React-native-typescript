const asyncKeys = {
  accessToken: 'accessToken',
  defaultToken: 'defaultToken',
  isLogIn: 'isLogIn',
  cartItems: 'cartItems',
  language: 'language',
  FCMToken: 'FCMToken',
};

export default asyncKeys;
